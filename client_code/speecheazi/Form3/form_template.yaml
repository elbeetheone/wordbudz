components:
- components:
  - components:
    - components:
      - event_bindings: {tick: timer_1_tick}
        layout_properties: {grid_position: 'RXILEV,AIKIDR'}
        name: timer_1
        properties: {interval: 0}
        type: Timer
      - layout_properties: {grid_position: 'HHHOSQ,XFTLHD'}
        name: spacer_2
        properties: {height: 20}
        type: Spacer
      - components:
        - components:
          - layout_properties: {grid_position: 'PTQWJO,HYLFAE'}
            name: spacer_1
            properties: {height: 89}
            type: Spacer
          - layout_properties: {grid_position: 'YXQAFR,RGXDOZ'}
            name: image_1
            properties: {display_mode: zoom_to_fill, height: 298, role: message2, source: _/theme/speech.jpg, spacing_above: large, spacing_below: large}
            type: Image
          - components:
            - layout_properties: {grid_position: 'LTRVWT,QYZPVV'}
              name: speak_icon_1
              properties: {}
              type: form:speecheazi.speak_icon
            - event_bindings: {click: stop_rec_click}
              layout_properties: {grid_position: 'TLMYHQ,ZZDFPI'}
              name: stop_rec
              properties: {background: '#c61010', foreground: 'theme:White', text: stop}
              type: Button
            layout_properties: {grid_position: 'ULMEJW,MHHUGE'}
            name: card_6
            properties: {role: message, visible: false}
            type: ColumnPanel
          - layout_properties: {grid_position: 'WODXAC,BYHIXB'}
            name: spacer_3
            properties: {height: 89}
            type: Spacer
          layout_properties: {grid_position: 'DLAQQR,ZJNYUB'}
          name: card_3
          properties: {background: 'theme:Black', col_widths: '{}', role: message2}
          type: ColumnPanel
        layout_properties: {grid_position: 'DMNOSI,HLCTVH XZYWJV,GZQYUN'}
        name: card_2
        properties: {background: 'theme:Black', role: message2}
        type: ColumnPanel
      - layout_properties: {grid_position: 'DMNOSI,HLCTVH VFDUGS,CEUMCD'}
        name: label_5
        properties: {align: center, bold: true, foreground: 'theme:Primary 700'}
        type: Label
      - layout_properties: {grid_position: 'DMNOSI,HLCTVH LUWGSR,FGHZCV'}
        name: label_2
        properties: {align: center, bold: true, foreground: 'theme:Primary 700'}
        type: Label
      - layout_properties: {grid_position: 'DMNOSI,IZTQTD SBVNID,YOJLLR'}
        name: headline_1
        properties: {align: center, bold: true, font_size: 35, foreground: 'theme:Primary 500', role: message, spacing_above: none, spacing_below: none, text: Topic of the Day, underline: true}
        type: Label
      - layout_properties: {grid_position: 'DMNOSI,IZTQTD BNSGNJ,SCIMVO'}
        name: label_4
        properties: {align: center, bold: true, font_size: 45, spacing_above: none, spacing_below: none}
        type: Label
      - event_bindings: {click: generate_click}
        layout_properties: {grid_position: 'DMNOSI,IZTQTD IZJBFS,TDRJKC'}
        name: generate
        properties: {background: 'theme:Primary 500', font_size: 45, role: primary-color, text: GENERATE}
        type: Button
      - layout_properties: {grid_position: 'DMNOSI,IZTQTD QSOKCP,RYBFXJ'}
        name: card_5
        properties: {role: message}
        type: ColumnPanel
      - event_bindings: {click: link_2_click}
        layout_properties: {grid_position: 'DMNOSI,IZTQTD TYKPGR,TULTKG'}
        name: link_2
        properties: {align: center, foreground: 'theme:Black', icon: 'fa:key', spacing_above: none, text: Tips, underline: true}
        type: Link
      - event_bindings: {click: share_click}
        layout_properties: {grid_position: 'DMNOSI,IZTQTD XYYPLU,NOBBJC'}
        name: share
        properties: {align: center, font_size: 30, icon: 'fa:share-alt', icon_align: top, text: ''}
        type: Link
      layout_properties: {grid_position: 'FYKKDF,YQDWQA'}
      name: content_panel_copy_copy
      properties: {col_widths: '{"HLCTVH":40,"IZTQTD":20,"YOJLLR":54,"GAWSFK":6}'}
      type: ColumnPanel
    layout_properties: {grid_position: 'WTRFAA,JXEADY'}
    name: content_panel_copy
    properties: {}
    type: ColumnPanel
  layout_properties: {slot: clip_playing_button}
  name: content_panel
  properties: {}
  type: ColumnPanel
container:
  properties:
    html: |-
      <!DOCTYPE html>
      <html lang="en">
        <head>
          <meta charset="UTF-8">
          <meta name="viewport" content="width=device-width, initial-scale=1.0">
          <title>Speech & Audio Control</title>
          <style>
            .texts {
              margin-top: 20px;
              display: none; /* Hide the texts div */
            }
            .disabled {
              pointer-events: none;
              opacity: 0.5;
            }
            audio {
              display: none;
            }
          </style>
        </head>
        <body>

          <div class="container">
            <div class="texts"></div>
            <div anvil-slot="voice_recording_button"></div>
            <div anvil-slot="clip_playing_button"></div>
          </div>

          <figure>
            <audio preload="auto" controlsList="nodownload" id="player">
              <source id="sourceMp3" src="" type="audio/mp3" />
              Your browser does not support the <code>audio</code> element.
            </audio>
          </figure>

          <script>
            (function() { // IIFE starts here

              const texts = document.querySelector(".texts");

              window.SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
              let recognition = new SpeechRecognition();
              recognition.interimResults = true;
              let isRecognitionStopped = false;
              let p = document.createElement("p");

              recognition.addEventListener("result", (e) => {
                texts.appendChild(p);
                const text = Array.from(e.results).map(result => result[0]).map(result => result.transcript).join("");
                p.innerText = text;
                if (e.results[0].isFinal) {
                  p = document.createElement("p");
                }
              });

              recognition.addEventListener("end", () => {
                if (!isRecognitionStopped) {
                  recognition.start();
                }
              });

              window.stopRecognition = function() {
                isRecognitionStopped = true;
                recognition.stop();
              };

              window.startRecognition = function() {
                isRecognitionStopped = false;
                recognition.start();
              };

              window.getAllTexts = function() { // Global getAllTexts
                const paragraphs = texts.querySelectorAll("p");
                let allText = "";
                paragraphs.forEach((paragraph) => {
                  allText += paragraph.innerText + " ";
                });
                texts.innerHTML = "";
                return allText.trim();
              };

            })(); // IIFE ends here

            // Load Clip Function (now only handles audio playback)
            window.loadClip = function(x) {
              var player = document.getElementById('player');
              window.stopRecognition();
              player.src = x;
              player.play();

              player.onended = function () {
                isRecognitionStopped = false;
                window.enableRecordingButton(); // Re-enable recording button after clip ends.
              };
            };

            // Attach event listeners for Anvil slots
            window.addEventListener('DOMContentLoaded', function() {
              let recordButton = document.querySelector("div[anvil-slot='voice_recording_button']");
              let clipButton = document.querySelector("div[anvil-slot='clip_playing_button']");

              if (recordButton) {
                recordButton.addEventListener("click", function() {
                  window.startRecognition();
                  // Enable clip button from anvil code.
                });
              }

              if (clipButton) {
                clipButton.addEventListener("click", function() {
                  let audioURL = "YOUR_AUDIO_URL_HERE"; // Replace with actual audio URL
                  if (audioURL.trim() !== "") {
                    window.loadClip(audioURL);
                    // Disable clip button from anvil code.
                  }
                });
              }
              if (clipButton){
                clipButton.classList.add('disabled'); //clip button starts disabled.
              }
            });

            // Anvil control functions
            window.enableRecordingButton = function() {
              let recordButton = document.querySelector("div[anvil-slot='voice_recording_button']");
              if (recordButton) recordButton.classList.remove("disabled");
            };

            window.disableRecordingButton = function() {
              let recordButton = document.querySelector("div[anvil-slot='voice_recording_button']");
              if (recordButton) recordButton.classList.add("disabled");
            };

            window.enableClipButton = function() {
              let clipButton = document.querySelector("div[anvil-slot='clip_playing_button']");
              if (clipButton) clipButton.classList.remove("disabled");
            };

            window.disableClipButton = function() {
              let clipButton = document.querySelector("div[anvil-slot='clip_playing_button']");
              if (clipButton) clipButton.classList.add("disabled");
            };

          </script>

        </body>
      </html>
  type: HtmlTemplate
is_package: true
